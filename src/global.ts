const deviceId = Math.random().toString(16).substr(2) + Math.random().toString(32).substr(2);

const iconType = 'arrow';

const fundAmount = {};
const showEarnings = 1;

export default {
  iconType,
  deviceId,
  fundAmount,
  showEarnings,
};
